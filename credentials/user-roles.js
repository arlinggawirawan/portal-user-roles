// Define an array of objects containing usernames, passwords, and access roles
const userCredentials = [
    { username: 'xxxxxawan@gpi.id', password: 'xxxxxxxxx', role: 'super-admin' }
    // add more account and roles for create scenario
];

// Export the array for use in other files
export default userCredentials;